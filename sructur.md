telegram-bot-moex/
├── 📁 cmd/                            # Точки входа приложения
│   └── 📁 bot/
│       └── main.go                    # Основной файл запуска бота (конфиг, graceful shutdown)
│
├── 📁 internal/                       # Внутренние пакеты приложения
│   ├── 📁 api/                        # Клиент для работы с API MOEX Fetcher
│   │   ├── client.go                  # HTTP клиент с методами для всех API эндпоинтов
│   │   └── types.go                   # Типы данных для API (в разработке)
│   │
│   ├── 📁 bot/                        # Основная логика Telegram бота
│   │   ├── bot.go                     # Основной тип Bot, инициализация и lifecycle методы
│   │   ├── commands.go                # Регистрация всех команд и routing сообщений
│   │   ├── handlers_basic.go          # Базовые команды (/start, /help, /status, etc)
│   │   ├── handlers_admin.go          # Админские команды (/config, /restart, /users, etc)
│   │   ├── handlers_instrument.go     # Команды для работы с инструментами
│   │   ├── handlers_turtle.go         # Команды стратегии "Черепах"
│   │   ├── handlers_steps.go          # Step-by-step обработчики для диалогов
│   │   ├── handlers_utils.go          # Вспомогательные функции для обработчиков
│   │   ├── types.go                   # Внутренние типы (UserState, BotStats, etc)
│   │   ├── utils.go                   # Утилиты бота (отправка сообщений, проверки)
│   │   └── help_callbacks.go          # Обработчики callback для меню помощи
│   │
│   ├── 📁 config/                     # Конфигурация приложения
│   │   ├── config.go                  # Структуры конфигурации и загрузка из YAML/env
│   │   ├── validation.go              # Валидация конфигурационных параметров
│   │   ├── helpers.go                 # Вспомогательные функции (пути, вывод конфига)
│   │   └── config_test.go             # Тесты для конфигурации
│   │
│   ├── 📁 analysis/                   # Анализ данных и стратегии
│   │   └── turtle_strategy.go         # Реализация стратегии "Черепах"
│   │
│   └── 📁 utils/                      # Общие утилиты
│       └── logger.go                  # Настройка логгера (zap + lumberjack)
│
├── 📁 pkg/                           # Пакеты, которые могут использоваться внешне
│   ├── 📁 commands/                  # (зарезервировано для будущего)
│   └── 📁 keyboards/                 # (зарезервировано для будущего)
│
├── 📁 static/                        # Статические файлы (зарезервировано)
│   ├── 📁 images/
│   └── 📁 templates/
│
├── 📁 configs/                       # Конфигурационные файлы
│   ├── config.yaml                   # Основной конфиг (с вашими данными)
│   └── config.yaml.example           # Пример конфига для разработки
│
├── 📁 scripts/                       # Скрипты развертывания
│   ├── deploy.sh                     # Скрипт развертывания на сервер
│   └── setup.sh                      # (отсутствует) Скрипт начальной настройки
│
├── 📁 logs/                          # Логи приложения (создается автоматически)
│   └── bot.log                       # Файл логов (ротация через lumberjack)
│
├── 📁 data/                          # Данные приложения (создается автоматически)
│   └── ...                          # Кэши, временные файлы
│
├── Dockerfile                        # Docker конфигурация для сборки образа
├── docker-compose.yml               # Docker Compose для запуска стека (бот + API + БД)
├── Makefile                         # Make команды для удобства разработки
├── go.mod                          # Go модули и зависимости
├── go.sum                          # Контрольные суммы зависимостей
├── README.md                       # Документация проекта
└── structure.md                    # Описание структуры проекта (этот файл)